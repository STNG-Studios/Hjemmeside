# AI-ændringer — Session 006 (2026-02-08)

## Resumé
Servicevilkår-siden (`servicevilkår.html`) blev oprettet med komplet indhold om vilkår for brug og privatlivspolitik. Layoutet følger FAQ-sidens design med individuelle bokse for hvert punkt, men uden expand/collapse-funktionalitet.

## Ændrede filer
| Fil | Handling | Beskrivelse |
|-----|----------|-------------|
| `servicevilkår.html` | Udfyldt (var tom) | Komplet side med nav, header, 8 vilkårs-bokse og footer |
| `css/style.css` | Redigeret | Tilføjet `.terms-*` CSS-klasser og responsive overrides |

## Detaljerede ændringer

### `servicevilkår.html` (udfyldt fra 0 bytes)
- Komplet HTML5-dokument med `<head>` (title, meta description, Open Graph tags)
- Identisk sticky header/nav kopieret fra faq.html (linje 23-43)
- `<section class="terms-header">` med titel, undertekst og intro-tekst (linje 49-53)
- `<section class="terms-list">` med 8 `<article class="terms-item">` bokse (linje 56-107)
- Punkt 5 (Databeskyttelse og Cookies) bruger `<ul class="terms-subpoints">` til underpunkter
- Identisk footer kopieret fra faq.html (linje 110-152)
- jQuery og main.js scripts (linje 155-156)

### `css/style.css` (redigeret)
- Tilføjet ny sektion "SERVICEVILKÅR-SEKTION" efter FAQ-sektionen (efter linje 631)
- `.terms-header` — centreret header med padding der tager højde for nav-height
- `.terms-header h1` — responsiv font-størrelse med clamp()
- `.terms-subtitle` og `.terms-intro` — sekundær tekstfarve
- `.terms-list` — flex column container med max-width 800px
- `.terms-item` — bokse med bg-secondary, border-radius 40px, squircle
- `.terms-item h2` og `.terms-item p` — titel- og indholdsstyling
- `.terms-subpoints` — liste-styling med disc bullets til punkt 5
- Responsive overrides i `@media (min-width: 768px)` — større padding

## AI's rolle
Simon bad om at oprette servicevilkår.html med et layout der ligner FAQ-siden, men med synlige bokse i stedet for accordion. Claude planlagde implementeringen, oprettede nye CSS-klasser (i stedet for at genbruge FAQ-klasser for at undgå kobling), og skrev alt indholdet som Simon leverede ind i de 8 sektioner.
